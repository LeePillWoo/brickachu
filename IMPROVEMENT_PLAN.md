# 브릭카츄 개선 계획서

> 분석 기준일: 2026-03-18
> 현재 코드베이스 파악 후 작성. 구현 난이도는 상/중/하 3단계.

---

## 목차

1. [교육적 개선](#1-교육적-개선)
2. [재미 개선](#2-재미-개선)
3. [시각적 개선](#3-시각적-개선)
4. [시뮬레이터 개선](#4-시뮬레이터-개선)
5. [성능 최적화](#5-성능-최적화)
6. [우선순위 요약](#6-우선순위-요약)

---

## 1. 교육적 개선

### 1-A. 블록 통계 HUD `[난이도: 하]`
**현황**: 현재 배치된 블록 수나 색상 구성을 알 수 없음
**개선안**:
- 우하단에 소형 통계 패널 추가
  - 총 블록 수 / 색상별 블록 수 파이차트
  - 배치한 블록 크기(복셀 기준) 표시 (예: "3×5×4 범위")
- 실시간 업데이트 (블록 배치/제거 시)

**관련 파일**: `ui.js`, `state.js` (`objects` 배열 기반)

---

### 1-B. 동물 정보 카드 `[난이도: 하]`
**현황**: 동물 클릭 시 스핀/점프 등 시각 효과만 있음
**개선안**:
- 클릭 시 우측 하단에 카드 팝업 (1.5초 후 자동 닫힘)
  - 동물 이름, 타입(포켓몬/동물), 그룹(HOP/HEAVY 등), 속도 배율
  - 포켓몬 타입이면 속성 아이콘(불꽃/물/전기 등) 색상으로 표시
  - "먹이까지 거리: NNN" 실시간 표시

**관련 파일**: `entities.js` (`ANIM_TYPE`, `SPEED_MULT` 활용), `ui.js`

---

### 1-C. 나노블록 조립 가이드 모드 `[난이도: 중]`
**현황**: 완전 자유 빌드만 가능
**개선안**:
- 피카츄 등 미리 정의된 도안을 반투명 가이드로 오버레이
- 가이드와 일치하는 블록 배치 시 초록 피드백, 틀리면 빨간 피드백
- 완성도 % 표시 (우측 패널 또는 프리뷰 패널 하단)
- 도안 데이터는 JSON으로 정의 (블록 위치 + 색상 슬롯 배열)

**관련 파일**: `scene.js` (`getFullSnapshot()` 구조 재사용), 신규 `guide.js`

---

### 1-D. 동물 수집 도감 `[난이도: 중]`
**현황**: 동물을 스폰해도 기록이 남지 않음
**개선안**:
- 📖 버튼 추가 → 팝업 도감 열기
- 스폰해본 동물은 컬러, 미스폰은 실루엣으로 표시
- 로컬스토리지에 수집 현황 저장 (새로고침 후에도 유지)
- 전체 동물 수 / 수집 수 진행 바 표시

**관련 파일**: `ui.js`, `entities.js` (`GROUP_ANIMALS.all` 기반)

---

## 2. 재미 개선

### 2-A. 동물 배고픔 + 에너지 시스템 `[난이도: 중]`
**현황**: 동물은 먹이를 먹지만 허기 상태가 없어 행동이 단조로움
**개선안**:
- 동물 객체에 `hunger` 필드 추가 (0~100, 시간이 지나면 감소)
- 배고프면 먹이를 더 공격적으로 추적, 포만 상태면 느긋하게 배회
- hunger = 0 되면 동작이 느려지고 앉음 (특수 애니메이션)
- HEAVY 동물은 hunger 감소 속도가 2배

**관련 파일**: `entities.js` (`updateDogs` 루프에 hunger 감소 추가)

---

### 2-B. 동물 번식 시스템 `[난이도: 중]`
**현황**: 동물은 스폰/제거만 있음
**개선안**:
- 같은 종 동물 2마리가 EAT_RADIUS × 2 이내에서 3초 이상 머물면 번식 조건 충족
- 번식 시 두 동물 사이에 작은(scale 0.4) 아기 버전 스폰
- 아기는 10초 동안 근처를 맴돌다 성체 크기로 성장 (tween)
- MAX_ANIMALS 도달 시 번식 불가 (이미 구현된 상한선 활용)

**관련 파일**: `entities.js`

---

### 2-C. 포식자-피식자 관계 강화 `[난이도: 중]`
**현황**: carnivore 그룹이 있지만 초식동물 추격이 없음
**개선안**:
- carnivore 동물은 `findNearestFood()` 대신 `findNearestPrey()` 사용
- FLEE_RADIUS(현재 voxelSize×6) 내 초식동물은 공포 상태로 전환 → 빠르게 도망
- 사냥 성공 시 효과음 + 파티클
- 블록으로 울타리를 만들면 초식동물 피난처 역할 (carnivore는 벽을 넘지 못함)

**관련 파일**: `entities.js` (`FLEE_RADIUS`, `probeAhead` 활용)

---

### 2-D. 이벤트 시스템 (랜덤 이벤트) `[난이도: 중]`
**현황**: 동물 행동이 단조롭고 예측 가능
**개선안**:
- 30~120초 랜덤 간격으로 이벤트 발생
  - **먹이 폭풍**: 사과 5개가 하늘에서 랜덤 위치에 낙하
  - **포켓몬 등장**: 특별 동물이 화면 테두리에서 등장 + 음악 변화
  - **무리 스폰**: 동일 종 3마리 동시 스폰 (특수 효과음)
  - **지진**: 기존 폭발 모드를 일시적으로 활성화
- 이벤트 알림 텍스트 화면 상단에 일시 표시 (2초)

**관련 파일**: `main.js` (animate 루프에 타이머), `ui.js`

---

### 2-E. 동물 이름 태그 & 말풍선 `[난이도: 하]`
**현황**: 동물들이 익명으로 돌아다님
**개선안**:
- 스폰 시 랜덤 이름 부여 (예: "피카츄 #3", "졸린 스노락스")
- 동물 클릭 시 머리 위에 CSS 말풍선 (HTML overlay, `getBoundingClientRect` 방식)
  - 짧은 대사: "냠냠!", "꼬르륵...", "쿵쾅!" (타입별로 다르게)
  - 1.5초 후 자동 소멸

**관련 파일**: `entities.js`, `input.js`, `ui.js`

---

### 2-F. 블록 완성 감지 + 챌린지 모드 `[난이도: 상]`
**현황**: 뭔가를 완성해도 아무런 피드백 없음
**개선안**:
- 미리 정의된 도형 패턴(하트, 별, 피카츄 등)과 현재 구조물 비교
- 70% 이상 일치 시 "완성 감지!" 팝업 + 컨페티 파티클
- 타이머 챌린지 모드: 제한 시간 내 도안 완성 → 점수 계산
- 점수는 로컬스토리지에 기록 (도전과제 형태)

**관련 파일**: `scene.js` (`getFullSnapshot()` 구조 비교), 신규 `challenge.js`

---

## 3. 시각적 개선

### 3-A. 낮/밤 사이클 `[난이도: 하]`
**현황**: 하늘색(0xe6f2ff) 고정, 빛 방향/강도 고정
**개선안**:
- 실시간 사이클 또는 버튼 토글 (☀️ / 🌙)
- 낮: 현재 상태
- 노을: DirectionalLight 오렌지/붉은색, 하늘 그라데이션
- 밤: AmbientLight 0.5로 감소, DirectionalLight 파란 달빛, 하늘 짙은 남색
- 동물들이 밤에는 느려지고 낮에는 활발해짐

```js
// main.js 기존 코드 활용
state.directionalLight.color.setHex(0xff8040); // 노을
state.scene.background = new THREE.Color(0x1a1a2e); // 밤하늘
```

**관련 파일**: `main.js` (animate 루프에 light 보간)

---

### 3-B. 날씨 파티클 시스템 `[난이도: 중]`
**현황**: 날씨 없음, 씬이 정적으로 느껴짐
**개선안**:
- 🌧️ 비: 위에서 아래로 떨어지는 얇은 LineSegments 파티클 수백 개
  - 동물 이동 속도 0.8× 감소
  - 착지 시 작은 스플래시 파티클
- ❄️ 눈: 천천히 내려오는 흰 SphereGeometry (작은 크기), 흔들리며 낙하
  - 블록 위에 쌓이는 효과 (블록 상단 색을 흰색으로 오버레이)
- 날씨 버튼을 UI 패널에 추가 (☀️→🌧️→❄️→☀️ 토글)

**관련 파일**: 신규 `weather.js`, `main.js`, `entities.js`

---

### 3-C. 블록 배치/제거 이펙트 강화 `[난이도: 하]`
**현황**: 블록이 순간적으로 나타나고 사라짐 (애니메이션 없음)
**개선안**:
- **배치**: 스케일 0→1 팝 애니메이션 (0.1초, `easeOutBack` 커브)
- **제거**: 스케일 1→0 축소 + 먼지 파티클 3~5개 방출
- **드래그 빌드**: 각 블록이 0.03초 간격으로 순차적으로 팝 인
- 음표 효과: 블록 배치 시 피치가 높이에 따라 변하는 톤 (이미 sound.js 있음)

**관련 파일**: `scene.js` (`placeVoxel` 함수), `sound.js`

---

### 3-D. Bloom 포스트프로세싱 `[난이도: 중]`
**현황**: SAOPass만 적용, 발광 재질(emissive)이 약하게 표현됨
**개선안**:
- Three.js `UnrealBloomPass` 추가 (이미 CDN에서 사용 가능)
- Charmander 꼬리 불꽃 (이미 `emissive: 0xff3d00`로 정의됨) glow 효과
- Gengar, 포르곤 등 special 그룹에 컬러 bloom 적용
- 낮/밤 사이클과 연동 (밤에 bloom 강도 증가)

```js
// main.js EffectComposer에 추가
import { UnrealBloomPass } from 'three/addons/postprocessing/UnrealBloomPass.js';
const bloomPass = new UnrealBloomPass(new THREE.Vector2(window.innerWidth, window.innerHeight), 0.4, 0.4, 0.85);
state.composer.addPass(bloomPass); // OutputPass 전에 삽입
```

**관련 파일**: `main.js`

---

### 3-E. 동물 발자국 이펙트 `[난이도: 중]`
**현황**: 동물이 이동해도 흔적 없음
**개선안**:
- HEAVY 동물 이동 시 바닥에 발자국 데칼 (PlaneGeometry, 반투명, 5초 후 fade out)
- 발자국은 왼발/오른발 교대로 이동 방향에 수직으로 배치
- 눈 날씨와 결합 시 흰 눈 위에 발자국이 뚜렷하게 보임
- Carnivore 발자국은 발톱 모양으로 다르게

**관련 파일**: `entities.js` (`updateDogs` 이동 로직 내)

---

### 3-F. 프리뷰 패널 개선 `[난이도: 하]`
**현황**: 프리뷰 씬에 조명 없고 배경이 단순 회색
**개선안**:
- 프리뷰 씬에도 약한 AmbientLight + DirectionalLight 추가
  → 현재 MeshBasicMaterial을 MeshPhongMaterial로 교체하면 깊이감 생김
- 프리뷰 패널 하단에 블록 크기 정보 표시 (X: N, Y: N, Z: N voxels)
- 프리뷰 자동 회전 토글 버튼 (빙글빙글 돌면서 보여주기)

**관련 파일**: `camera.js` (`updatePreview()`), `main.js` (`initPreview()`)

---

## 4. 시뮬레이터 개선

### 4-A. 생태계 HUD `[난이도: 하]`
**현황**: 동물/먹이 수를 UI에서 확인 불가
**개선안**:
- 화면 우상단에 미니 HUD:
  ```
  🐾 동물: 7 / 20   🍎 먹이: 3   💀 포식: 12회
  ⏱ 경과: 04:32
  ```
- 포식 성공 카운터 (carnivore가 prey를 잡을 때마다 증가)
- 경과 시간 타이머

**관련 파일**: `ui.js`, `entities.js`

---

### 4-B. 환경 파라미터 슬라이더 `[난이도: 중]`
**현황**: 동물 속도/먹이 관련 파라미터가 코드에 하드코딩됨
**개선안**:
- lil-gui (이미 사용 중)에 새 폴더 "Ecosystem" 추가:
  - **먹이 자동 스폰** (토글 + 간격: 5~60초)
  - **동물 이동 속도** (0.5× ~ 3×)
  - **최대 동물 수** (1~30)
  - **포식자 공격력** (벽 파괴 블록 수 0~5)
- 슬라이더 변경 시 `entities.js` 상수에 반영

**관련 파일**: `ui.js` (`setupGUI()`), `entities.js` (상수를 `state`로 이동)

---

### 4-C. 동물 상태바 (머리 위 HUD) `[난이도: 중]`
**현황**: 동물 내부 상태(배고픔 등)가 시각화되지 않음
**개선안**:
- 동물 머리 위 HTML overlay로 미니 상태 표시 (CSS absolute positioning)
  - 배고픔 바 (초록→노랑→빨강)
  - 이름 태그
  - 상태 아이콘 (😴 idle / 🏃 walking / 😱 fleeing / 😋 eating)
- 카메라 이동 시 3D→2D 투영으로 위치 업데이트 (매 프레임 `Vector3.project()`)
- 토글 버튼으로 전체 HUD 표시/숨김

**관련 파일**: `entities.js`, `ui.js`, `main.js` (animate 루프에 투영 업데이트)

---

### 4-D. 구조물 저장/불러오기 `[난이도: 중]`
**현황**: 새로고침하면 구조물이 사라짐
**개선안**:
- 💾 버튼: 현재 블록 구조물을 JSON 파일로 다운로드
  - `scene.js`의 `getFullSnapshot()` 포맷 그대로 사용
- 📂 버튼: JSON 파일 drag & drop 또는 파일 선택으로 불러오기
  - `applyActionState()` 함수 재사용
- 브라우저 localStorage에 자동 저장 (5분마다, 또는 블록 배치 시)

**관련 파일**: `scene.js`, `ui.js`

---

### 4-E. 자동 생태계 시뮬레이션 모드 `[난이도: 상]`
**현황**: 플레이어가 직접 동물을 스폰해야 함
**개선안**:
- 🌿 "자동 모드" 토글: 켜면 플레이어 개입 없이 자동으로
  - 일정 간격으로 동물 스폰 (개체 수 균형 유지)
  - 먹이 자동 스폰 (먹이 부족 시 더 자주)
  - 동물이 너무 많으면 번식 억제
- 개체 수 변화 그래프 (canvas 기반 미니 차트, 우하단)
  - X축: 시간, Y축: 동물 수 / 먹이 수 (색상으로 구분)
- 포식자/피식자 균형이 깨지면 자동으로 보정 스폰

**관련 파일**: 신규 `ecosystem.js`, `main.js`

---

## 5. 성능 최적화

### 5-A. InstancedMesh로 블록 렌더링 `[난이도: 상]`
**현황**: 블록 하나당 개별 `THREE.Mesh` 생성 → draw call이 블록 수만큼 증가
**개선안**:
- `THREE.InstancedMesh`를 사용해 같은 색상 블록을 하나의 draw call로 처리
- `objects` 배열 대신 색상 슬롯별 InstancedMesh 유지
- 블록 1000개 기준 draw call: 1000개 → 색상 수(최대 74개)로 감소

> **주의**: `input.js`의 Raycaster 충돌 감지, `entities.js`의 지형 탐지가 모두 개별 Mesh 기반이라 대규모 리팩토링 필요. 가장 난이도가 높고 신중하게 접근해야 함.

---

### 5-B. 동물 AI Raycaster 최적화 `[난이도: 중]`
**현황**: 매 프레임 모든 동물에 대해 `probeAhead()`, `getGroundHeightBelow()` 호출
**개선안**:
- 동물당 AI 업데이트를 4프레임마다 1회로 throttle
  ```js
  if ((frameCount + animal._aiOffset) % 4 !== 0) { /* skip AI */ }
  ```
- `_aiOffset`은 스폰 시 랜덤 설정 → 동물마다 업데이트 타이밍 분산
- 동물 20마리 기준 Raycaster 호출 75% 감소

**관련 파일**: `entities.js` (`updateDogs` 함수)

---

### 5-C. 동물 Geometry 재사용 `[난이도: 중]`
**현황**: 동물 스폰 시마다 `new THREE.BoxGeometry()`를 각 파트마다 새로 생성
**개선안**:
- 자주 쓰이는 크기(예: `voxelSize`의 특정 배율)의 geometry를 캐시로 저장
  ```js
  const _geoCache = {};
  function getCachedGeo(w, h, d) {
      const key = `${w}_${h}_${d}`;
      if (!_geoCache[key]) _geoCache[key] = new THREE.BoxGeometry(w, h, d);
      return _geoCache[key];
  }
  ```
- 동물 20마리 스폰 시 geometry 객체 수: ~200개 → ~20~30개로 감소

**관련 파일**: `entities.js` (`_buildAnimalMesh()`)

---

## 6. 우선순위 요약

| 우선순위 | 항목 | 기대 효과 | 난이도 |
|----------|------|-----------|--------|
| ⭐⭐⭐ | 3-A 낮/밤 사이클 | 분위기 즉각 변화, 코드 간단 | 하 |
| ⭐⭐⭐ | 3-C 블록 이펙트 강화 | 빌드 피드백 향상 | 하 |
| ⭐⭐⭐ | 4-D 저장/불러오기 | 실용성 최고, 기존 코드 재사용 | 중 |
| ⭐⭐⭐ | 4-A 생태계 HUD | 시뮬레이터 느낌 즉각 강화 | 하 |
| ⭐⭐ | 2-A 배고픔 시스템 | 동물 행동 다양성 증가 | 중 |
| ⭐⭐ | 2-E 이름 태그 & 말풍선 | 감성 + 재미 | 하 |
| ⭐⭐ | 3-D Bloom 포스트프로세싱 | 발광 동물 비주얼 극적 개선 | 중 |
| ⭐⭐ | 4-B 환경 파라미터 슬라이더 | 실험 가능성 확장 | 중 |
| ⭐⭐ | 2-C 포식자-피식자 강화 | 생태계 긴장감 | 중 |
| ⭐⭐ | 5-B AI Raycaster 최적화 | 동물 많을 때 프레임 안정 | 중 |
| ⭐ | 1-B 동물 정보 카드 | 교육적 가치 | 하 |
| ⭐ | 3-B 날씨 파티클 | 분위기 풍부화 | 중 |
| ⭐ | 4-E 자동 생태계 모드 | 완성도 높은 시뮬레이터 | 상 |
| ⭐ | 5-A InstancedMesh | 대규모 빌드 성능 | 상 |

---

## 구현 추천 순서 (단계별)

### 1단계 — 빠른 성과 (각 1~2시간)
1. **3-A 낮/밤 사이클** — animate 루프에 light 보간만 추가
2. **4-A 생태계 HUD** — HTML 요소 + animals.length 표시
3. **2-E 이름 태그** — 스폰 시 랜덤 이름, CSS 말풍선
4. **3-C 블록 배치 이펙트** — placeVoxel에 scale 0→1 tween 추가

### 2단계 — 시뮬레이터 완성도 (각 3~5시간)
5. **4-D 저장/불러오기** — getFullSnapshot/applyActionState 재사용
6. **2-A 배고픔 시스템** — animal 객체에 hunger 필드 + updateDogs 수정
7. **4-B 환경 슬라이더** — setupGUI()에 Ecosystem 폴더 추가
8. **5-B AI 최적화** — throttle 4프레임, aiOffset 분산

### 3단계 — 비주얼 & 재미 완성 (각 5~8시간)
9. **3-D Bloom Pass** — UnrealBloomPass 한 줄 추가 + emissive 재질 강화
10. **2-C 포식자-피식자** — carnivore용 findNearestPrey 함수
11. **3-B 날씨 파티클** — weather.js 신규 모듈
12. **2-D 랜덤 이벤트** — 타이머 기반 이벤트 시스템

### 4단계 — 심화 (각 1~2일)
13. **1-C 조립 가이드 모드**
14. **4-E 자동 생태계 모드**
15. **5-A InstancedMesh 리팩토링**
