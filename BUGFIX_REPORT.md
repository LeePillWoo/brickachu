# 버그 진단·수정 기록

점검일: 2026-09-21

기존 게임의 동물 종류와 능력을 유지하면서, 재현하거나 코드·물리 검증으로 확인한 오류를 수정했습니다.

## 수정 내용

| 영역 | 수정한 동작 |
| --- | --- |
| 블록 입력 | 한 번 클릭하면 한 개만 배치. 우클릭·휠·Alt 회전은 편집하지 않음. UI 위에서 놓은 드래그, 취소된 입력, 두 번째 터치가 블록을 만들지 않음. |
| 모드·단축키 | 동물 제거 모드에서 블록 편집 차단. 폼 입력 중 게임 단축키 차단. 한글 입력 상태에서도 물리 키 위치로 이동. ESC·창 이탈·포인터 취소 시 입력과 잡기 상태 정리. |
| 긴누르기·터치 | 동물 그룹 메뉴가 손을 뗀 뒤에도 유지. 전체 삭제 버튼 밖 이동·입력 취소·다른 모드 전환 시 타이머 취소. 도움말 중복 토글과 모바일 메뉴 화면 넘침 수정. |
| 실행 취소·복원 | 폭발 직전 구조물 전체를 정확히 복구. 모든 팔레트 슬롯의 색상·거칠기 보존. 중복 삭제가 다른 블록을 지우지 않음. 프리뷰·물리 바디도 함께 정리. |
| 지면·카메라 | 신규·복원 블록의 월드 좌표를 즉시 갱신하여 렌더 전 지면 검사 정상화. 큰 구조물 프리뷰와 세로 화면 F 포커스 잘림 수정. 화면 크기 변경 시 후처리 크기 동기화. AO 반경 설정을 실제 라이브러리 속성에 연결. |
| 게임 시간 | 30·60·144Hz에서 같은 게임 속도. ×2·×3에서도 물리 충돌 계산은 1/60초 단위 유지. 탭 복귀 시 과도한 시간 누적 방지. 특수능력 화면 흔들림이 카메라 위치를 영구적으로 바꾸지 않음. |
| 동물 생성·이동 | 동물 추첨 목록 중복 제거. 육식동물 제한에 걸릴 때 기존 동물을 먼저 삭제하던 문제 수정. 높은 구조물 내부 스폰, 도주 중 보드 이탈, 대시 취소, 과도한 벽 점프, HEAVY 벽 파괴 누락 수정. |
| 동물 잡기·먹기 | 잡힌 동물의 보이지 않는 물리 몸체가 계속 떨어지던 문제와 이전 등반 상태 재개 방지. 코끼리의 바닥 먹이 섭취 불가, 벽 너머 먹이 소비 수정. 8종 모델의 발이 바닥에 묻히는 문제 수정. |
| 먹이 | 받침 블록 삭제·폭발·Undo 후 낙하. 고속 낙하가 얇은 플랫폼을 관통하지 않음. 바닥 높이를 물리 바닥과 일치. 정상 먹이 클릭 제거, 소비 완료와 제거 효과 중 전체 삭제의 중복 처리 수정. |
| 리소스·효과음 | 동물·먹이·연기·블록 프리뷰·파편의 소유 리소스 해제. 공유 블록 재질·기하 보존. 오디오 생성/재개 실패 처리, 음량 0 예약 방지, 재생 완료 오디오 노드 연결 해제. |

## 검증 결과

- **Node 회귀 52개 통과**: 동물 15, 블록·카메라 9, 입력 11, 효과음 5, 먹이·시간 계산 12.
- **Chromium 브라우저 검증 24개 통과**: 실제 페이지 초기화, 클릭·Undo/Redo·폭발·복원, 먹이 낙하, 동물 능력, 긴누르기, 모바일 탭·회전, 화면 크기 변경, 주사율·배속·카메라 흔들림.
- 동물 33종 각각에 대해 실제 Three.js/Cannon-es로 낙하·착지·바닥 먹이 섭취 확인.
- JS 구문 검사, `git diff --check`, 로컬 HTTP 서버 실행 검사 통과.

브라우저 검증은 헤드리스 Chromium과 모바일 터치 에뮬레이션으로 수행했습니다. 실제 iOS Safari/Android 단말의 오디오 정책·GPU 성능과 장시간 플레이는 별도 실기기 검증 대상입니다.

## 다시 실행하기

```powershell
node serve.mjs
node tests/run.mjs
```

게임 주소는 <http://127.0.0.1:8080>입니다. 서버는 실행 중인 터미널에서 `Ctrl+C`로 종료합니다. 테스트는 별도 터미널에서 실행합니다. 브라우저 검증 환경과 명령은 [tests/README.md](tests/README.md)를 참고하세요.
